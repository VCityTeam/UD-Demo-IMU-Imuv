### Underlying mongo database

MONGO_INITDB_ROOT_USERNAME=<mongo db root account name>
MONGO_INITDB_ROOT_PASSWORD=<associated password>
MONGO_PORT=27017

### Exposed parse server

PARSE_SERVER_FQDN=<the deployment server fully qualified domain name. Default: mongodb>
PARSE_APP_ID=<your application identifier> # also use in IMUV to connect to parse service
PARSE_MASTER_KEY=<some long and cryptic and stong generated key> # also use in IMUV to connect to parse service
PARSE_PORT=1337

### Exposed imuv

IMUV_PORT=443

### Exposed wbo

WBO_PORT=5001

# Used by IMUV

JITSI_PUBLIC_URL=<the url of the jitsi server. Default: https://meet.jit.si>
WBO_PUBLIC_URL=<the url of the whitebophir server. Default: http://localhost:5001/boards>
PARSE_SERVER_URL=<the url of the parse server. Default: http://parse-server:1337/parse>
